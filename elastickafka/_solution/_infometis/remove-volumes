#!/usr/bin/env bash



  ######################################################
  # THIS FILE IS AUTO-GENERATED - PLEASE DO NOT CHANGE #
  ######################################################

#!/usr/bin/env bash

executionplane --silent docker container prune --force
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__elastic_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__elastic_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__grafana_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__grafana_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__kafka_secrets_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__kafka_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__kafka_data_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__kafka_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__ksqldb_queries_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__ksqldb_queries_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__schema_registry_secrets_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__schema_registry_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__streamsets_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__streamsets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__zookeeper_secrets_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__zookeeper_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__zookeeper_data_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__zookeeper_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "hkvb_poc_elastickafka__zookeeper_log_volume"
	result=$(docker volume ls | grep "hkvb_poc_elastickafka__zookeeper_log_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
